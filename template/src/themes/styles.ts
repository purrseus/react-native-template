import { StyleSheet } from 'react-native';

export const globalStyles = StyleSheet.create({
  flexFill: {
    flex: 1,
  },
  flexCenter: {
    justifyContent: 'center',
    alignItems: 'center',
  },
  flexFillCenter: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
